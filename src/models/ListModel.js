define(['Backbone'], function(Backbone){    ListModel = Backbone.Model.extend({        defaults: {            status: 'unchecked'        },        validate: function(attr) {            if(attr.title === undefined) return 'error';            //Check if string only contains spaces            attr.title = attr.title.replace(/^\s+|\s+$/g, '');            if(attr.title.length === 0 || attr.title.lengt > 100) return 'error';        }    });    return ListModel;});