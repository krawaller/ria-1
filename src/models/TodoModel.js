define(['Backbone'], function(Backbone){	TodoModel = Backbone.Model.extend({        defaults: {            status: 'unchecked'        },        validate: function(attr) {            if(attr.todo === undefined) return 'error';            //Check if string only contains spaces            attr.todo = attr.todo.replace(/^\s+|\s+$/g, '');            if(attr.todo.length === 0 || attr.todo.lengt > 100) return 'error';        }    });	return TodoModel;}); 